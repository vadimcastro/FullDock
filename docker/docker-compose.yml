# template project-specific overrides

services:
  # Override database name for template project
  db:
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-template}
      
  # Override API database connection
  api:
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-template}
